{
  "entities": {
    "systemConfig": {
      "title": "System Configuration",
      "description": "Global gold prices, items, visibility/order, and admin settings for ORO Gold System",
      "type": "document",
      "path": "system/oro_config",
      "properties": {
        "rates": {
          "type": "map",
          "description": "Gold prices per gram for 24k, 21k, 18k"
        },
        "items": {
          "type": "array",
          "description": "List of gold items with weight, making fee, cashback, active status, and sort order"
        },
        "settings": {
          "type": "map",
          "description": "System title, admin PIN, rounding mode, auto-simulation toggle and interval"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of last update"
        }
      }
    }
  }
}
